import { StyleSheet } from "react-native";

const MessageStyle = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20
    },
    role: {
        fontWeight: 'bold',
        fontSize: 16,
        color:'black'
    }
})


export default MessageStyle;