import { StyleSheet } from "react-native";

const MessageDetailStyle = StyleSheet.create({
    contianer: {
        flex: 1,
        padding: 20
    },
    input: {
        padding: 10,
        borderWidth: 1,
        borderColor: 'gray',
        marginBottom: 10,
        paddingHorizontal: 10,
        borderRadius: 10,
    }
})

export default MessageDetailStyle;